class Solution {
public:
    bool canJump(vector<int>& nums) {
        int maxfind=0;

        for(int i=0;i<nums.size();i++)
        {
            if(maxfind<i)
                return false;
            
            maxfind=max(maxfind, nums[i]+i);
        }
        return true;
    }
};
